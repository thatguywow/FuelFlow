node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
.DS_Store
.env
.env.local

# Capacitor native projects are generated; keep them out until you commit
# platform-specific config deliberately.
android/
ios/

# Generated food data. Built by scripts/ or by CI and uploaded straight into
# the Pages artifact — deliberately never committed, so a multi-hundred-megabyte
# food database stays out of git history.
public/data/core-foods.json
public/fooddb/
scripts/.cache/
data-build/

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local screenshot harness (Playwright); not part of the build
tools/
shots/
